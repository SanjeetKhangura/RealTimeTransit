[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"
[project]
name = "realtimetransit_ingest"
version = "0.0.0a0"
dependencies = [
  "httpx",
  "hatch",
  "requests",
  "psycopg",
  "gtfs-realtime-bindings",
]