# Base URL of the Go API. Leave this unset for local development, which serves
# mock data in the browser via MSW. Set it to run against the real API:
#
#   NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
#
# Copy this file to .env.local and uncomment the line above when the API is running.
